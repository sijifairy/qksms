package com.moez.QKSMS.common.ad.nativead;

import android.content.Context;
import android.util.AttributeSet;
import android.widget.FrameLayout;

public class NmNativeAdIconView extends FrameLayout {

  public NmNativeAdIconView(Context context) {
    this(context, null);
  }

  public NmNativeAdIconView(Context context, AttributeSet attrs) {
    this(context, attrs, 0);
  }

  public NmNativeAdIconView(Context context, AttributeSet attrs, int defStyle) {
    super(context, attrs, defStyle);
  }
}
